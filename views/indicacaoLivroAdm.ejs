<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Usuário</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <style>
        body {
            margin: 0;
            font-family: 'Roboto', sans-serif;
            background-color: #193361;
            color: white;
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Garante que o footer fique na parte inferior */
            background-image: linear-gradient(rgba(0, 13, 56, 0.5), rgba(0, 13, 56, 0.5));
        }

        header {
            position: relative;
            z-index: 1000;
        }

        .container {
            flex-grow: 1; /* Permite que o conteúdo ocupe o espaço restante */
        }

        .titulo {
            border-bottom: 3px solid white;
            margin-bottom: 20px;
            padding: 24px;
            text-align: center;
            color: #fff;
        }

        .titulo h1 {
            margin: 0;
            font-weight: bold;
            font-size: 36px;
        }

        .list-group {
            padding: 0 10px;
            margin-bottom: 20px;
        }

        .list-group-item {
            background-color: #ffffff;
            color: #003366;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 10px;
            display: flex; /* Adiciona flex */
            justify-content: space-between; /* Alinha os itens com espaço entre */
            align-items: center; /* Centraliza verticalmente */
            position: relative;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .list-group-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .botao-ver {
            background-color: #003366;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .botao-ver:hover {
            background-color: #002244;
        }

        .sem-resultados {
            text-align: center;
            color: #ffffff;
        }

        footer {
            margin-top: auto; /* Faz com que o footer fique na parte inferior */
        }

        .barra-pesquisa {
            display: flex;
            justify-content: flex-end; /* Alinha o botão de pesquisa à direita */
            margin-top: 20px;
        }

        .barra-pesquisa input {
            padding: 10px;
            border-radius: 5px 0 0 5px;
            border: none;
            width: 100%; /* Faz o input ocupar todo o espaço disponível */
            max-width: 200px; /* Define um tamanho máximo */
            font-size: 20px;
        }

        .caixa-icone-pesquisa {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            background-color: rgb(151, 151, 151);
            border-radius: 0 5px 5px 0;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border: none;
        }

        .caixa-icone-pesquisa i {
            color: #ffffff;
        }

        .caixa-icone-pesquisa:hover {
            background-color: rgb(94, 94, 94);
        }
    </style>
</head>

<body>
    <header>
        <%- include('./navbarAdm.ejs', { activePage: 'indicacaoLivroAdm' }) %>
    </header>

    <div class="container">
        <div class="titulo">
            <h1>Indicações Livro</h1>
        </div>

        <div class="barra-pesquisa">
            <form class="d-flex" method="get" action="/pesquisarIndicacaoLivroAdm">
                <input class="form-control" name="pesquisa" type="search" placeholder="Pesquisar" aria-label="Search" />
                <button class="caixa-icone-pesquisa">
                    <i class="fa fa-search"></i>
                </button>
            </form>
        </div>

        <hr style="background-color: #fff;">
        <div class="list-group">
            <% indicacao.forEach((indicacao) => { %>
                <a href="/infoIndicacaoLivroAdm?cod_indicacao=<%= indicacao.cod_indicacao %>" class="list-group-item list-group-item-action">
                    <div>
                        <h5 class="mb-1"><%= indicacao.titulo %></h5>
                        <small><%= indicacao.autor %></small>
                    </div>
                    <button class="botao-ver">Ver</button>
                </a>
            <% }); %>
        </div>
        <% if (indicacao.length === 0) { %>
            <div class="sem-resultados">
                <p>Nenhuma indicação encontrada.</p>
            </div>
        <% } %>
    </div>

    <footer>
        <%- include('./footer.ejs', { activePage: 'indicacaoLivroAdm' }) %>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
